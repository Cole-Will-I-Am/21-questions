{
  "name": "echo-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Echo backend — Cloudflare Worker + D1. Adaptive 21-question self-portrait engine over Ollama Cloud.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute echo-arena --remote --file schema.sql"
  },
  "devDependencies": { "wrangler": "^4.0.0" }
}
